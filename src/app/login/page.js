import Login from "@/app/login/Login";

export const metadata = {
  title: "Login — Mubassir",
  description: "Sign in to your portfolio dashboard.",
};

export default function LoginPage() {
  return <Login />;
}