import AdminDashboard from "./AdminDashboard";

export const metadata = {
  title: "Admin Dashboard — Mubassir",
  description: "Manage your portfolio projects.",
};

export default function AdminPage() {
  return <AdminDashboard />;
}